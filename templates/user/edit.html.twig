{# {% extends 'base.html.twig' %}

{% block title %}Modification utilisateur{% endblock %}

{% block body %}
    <h1>Modifier utilisateur</h1>


    {{ include('user/_delete_form.html.twig') }}</br></br>
    {{ include('user/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    
    <form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">

        <a class="buy-now" href="{{ path('app_user_index') }}">Revenir à la liste</a>

        <button class="suppression-button"><a class="text-center, buy-now" >Supprimer</a></button>
        
    </form>
{% endblock %} #}


{% extends 'base.html.twig' %}

{# {% block title %}Modifier mes informations
{% endblock %}


{% block body %}
	{{ form_start(form) }}
	<section class="connection">
		<div
			class="container">

			<!-- MODIFIER -->
			<form method="post">

				<br/>
				<h1 class="title_connexion">Modifier mes informations</h1>
				<br/>
				{{ form_widget(form._token) }}

				{{ form_label(form.firstname) }}
				{{ form_widget(form.firstname, {'attr': {'class': 'form-control', 'autofocus': 'autofocus'}}) }}

				{{ form_label(form.lastname) }}
				{{ form_widget(form.lastname, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.email) }}
				{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.birthdate) }}
				{{ form_widget(form.birthdate, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.password) }}
				{{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
			</form>
		<div style="text-align: center;"></br>
		<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
			<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
			<button class="suppression-button">

				<a class="text-center, buy-now">Supprimer</a>
				<a class="buy-now" href="{{ path('app_user_index') }}">Revenir à la liste</a>
			</button>
		</form>
	</div>
			{{ form_end(form) }}
		</div>
</section>
{% endblock %} #}


{# {% block body %}
	<section class="connection">
		<div class="container">
			<form method="post">
				<br/>
				<h1 class="title_connexion">Modifier info Utilisateur</h1>
				<br/>

				{{ form_widget(form._token) }}

				{{ form_label(form.firstname) }}
				{{ form_widget(form.firstname, {'attr': {'class': 'form-control', 'autofocus': 'autofocus'}}) }}

				{{ form_label(form.lastname) }}
				{{ form_widget(form.lastname, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.email) }}
				{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.birthdate) }}
				{{ form_widget(form.birthdate, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.password) }}
				{{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}

				{# {{ form_end(form) }} #}

				{# <div style="text-align: center;"></br>
				<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
					<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
					<button class="suppression-button">

						<a class="text-center, buy-now">Supprimer</a>
						<a class="buy-now" href="{{ path('app_user_index') }}">Revenir à la liste</a>
                        <button type="submit" class="buy-now">Sauvegarder</button>


					</button>
				</form>
			</div>
		</form>
	</div>
</section>
{% endblock %}#}


{% block title %}Modification utilisateur
{% endblock %} 


{% block body %}
	{{ form_start(form) }}
	<section class="connection">
		<div class="container">
			<form method="post">

				<br/>
				<h1 class="title_connexion">Modification utilisateur</h1>
				<br/>
				{{ form_widget(form._token) }}

				{{ form_label(form.firstname) }}
				{{ form_widget(form.firstname, {'attr': {'class': 'form-control', 'autofocus': 'autofocus'}}) }}

				{{ form_label(form.lastname) }}
				{{ form_widget(form.lastname, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.email) }}
				{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.birthdate) }}
				{{ form_widget(form.birthdate, {'attr': {'class': 'form-control'}}) }}

				{{ form_label(form.password) }}
				{{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
               
                <div style="text-align: center;">
                    </br>
                    {{ include('user/_form.html.twig') }}
                    <a class="buy-now" href="{{ path('app_user_index') }}">Revenir à la liste</a>
                </div>

			</form>
			{{ form_end(form) }}
		</div>
	</section>

{% endblock %}





{# {% block body %}
				<section class="connection">
					<div
						class="container">
						<!-- MODIFIER -->
						<form method="post">
			
							<h1 class="title_connexion">Modifier mes informations</h1>
							<br/>
							<div class="form-floating mb-3">
								<input type="text" name="prenom" id="prenom" class="form-control" placeholder="Prénom" value="{{ app.user.firstname }}" required autofocus>
								<label for="prenom">Prénom</label>
							</div>
							<div class="form-floating mb-3">
								<input type="text" name="nom" id="nom" class="form-control" placeholder="Nom" value="{{ app.user.lastname }}" required>
								<label for="nom">Nom</label>
							</div>
							<div class="form-floating mb-3">
								<input type="email" name="email" id="inputEmail" class="form-control" placeholder="Adresse mail" value="{{ app.user.email }}" required>
								<label for="inputEmail">Adresse mail</label>
							</div>
							<div class="form-floating mb-3">
								<input type="date" name="date_naissance" id="date_naissance" class="form-control" placeholder="Date de naissance" value="{{ app.user.birthdate|date('Y-m-d') }}" required>
								<label for="date_naissance">Date de naissance</label>
							</div>
							<div class="form-floating mb-3">
								<input type="password" name="password" id="inputPassword" class="form-control" placeholder="Mot de passe" required>
								<label for="inputPassword">Mot de passe</label>
							</div>
						</form>
			
						<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
							<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
			
							<a class="buy-now" href="{{ path('app_user_index') }}">Revenir à la liste</a>
			
							<button class="suppression-button">
								<a class="text-center, buy-now">Supprimer</a>
							</button>
			
						</form>
			
					</div>
				</section>
			
			{% endblock %}
			
			{% block body %}
			
				<section class="connection">
					<div class="container">
						<div class="row justify-content-center">
							<div class="col-md-6">
								<div class="card">
									<div class="card-body">
										<h5 class="card-title">Modifier mes informations</h5>
										{{ form_start(form) }}
										<div class="form-group">
											{{ form_label(form.firstname) }}
											{{ form_widget(form.firstname, {'attr': {'class': 'form-control', 'autofocus': 'autofocus'}}) }}
										</div>
										<div class="form-group">
											{{ form_label(form.lastname) }}
											{{ form_widget(form.lastname, {'attr': {'class': 'form-control'}}) }}
										</div>
										<div class="form-group">
											{{ form_label(form.email) }}
											{{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
										</div>
										<div class="form-group">
											{{ form_label(form.birthdate) }}
											{{ form_widget(form.birthdate, {'attr': {'class': 'form-control'}}) }}
										</div>
										<div class="form-group">
											{{ form_label(form.password) }}
											{{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
										</div>
										{{ form_widget(form._token) }}
										<button type="submit" class="btn btn-primary">{{'Sauvegarder'}}</button>
										{{ form_end(form) }}
									</div>
								</div>
							</div>
						</div>
					</div>
				</section>
			    {% endblock %} #}
