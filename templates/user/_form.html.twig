{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="buy-now">{{ button_label|default('Sauvegarder')}}</button>
{{ form_end(form) }}
