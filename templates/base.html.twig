<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta
		name="viewport" content="width=device-width, initial-scale=1.0">

		{# <!DOCTYPE html>
						<html>
						<head>
						<meta charset="UTF-8"> #}
		<title>
			{% block title %}🌸Atelier Pauline🌸
			{% endblock %}
		</title>
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}


		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootswatch@5.1.3/dist/lux/bootstrap.min.css">

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
		<nav class="navbar navbar-expand-lg navbar-dark">
			{# <a class="#" href="{{path('home')}}"><img class="logobarre" alt="logo"></a> #}
			<div class="container-fluid">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>

				<div class="collapse navbar-collapse" id="navbarColor01">
					<ul class="navbar-nav me-auto">

						<li class="nav-item">
							<a class="navtitle" href="{{path('home')}}">ACCUEIL</a>
						</li>


						{# LISTE DEROULANTE CRUD#}
						{% if is_granted ('ROLE_ADMIN') %}
							<li class="nav-item">
								<div class="dropdown">
									<span class="navtitle">REGLAGES</span>
									<div class="dropdown-content">
										<a class="subCategory" href="{{path('app_user_index')}}">UTILISATEURS</a>
									</div>
								</div>
							</li>
						{% endif %}

						{# CONNEXION DECONNEXION CREER SON COMPTE #}
						<li class="nav-item">
							{% if is_granted ('ROLE_USER') %}

								<div class="dropdown">
									<a class="navtitle" href="{{path('app_user_index')}}">
										{{app.user.firstname}}</a>
									<div class="dropdown-content">
										{# <a class="subCategory" href="{{path('utilisateur', {id:app.user.id})}}">UTILISATEUR</a> #}
										<a class="subCategory" href="{{path('app_logout')}}">DECONNEXION</a>
									</div>
								</div>
							</li>

						{% else %}
							<a class="navtitle" href={{path('app_login')}}>COMPTE</a>
						{% endif %}
					</ul>

					{# BARRE DE RECHERCHE #}
					<form class="d-flex">
						<input class="form-control me-sm-2" type="text" placeholder="Search">
						<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
					</form>
				</div>
			</div>
		</nav>
		{% block body %}{% endblock %}


		<!--===== FOOTER =====-->
		{# <footer>
			<div>
				<ul class="mentions">
					<li>
						<a href="#">Politique de confidentialité</a>
					</li>
					<li>
						<a class="footer__copy">&#169; atelier Pauline Rousseau</a>
					</li>
				</ul>
				<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
			</body>
		</html>
	</body>
</html></div></footer> #}

</body></html>
